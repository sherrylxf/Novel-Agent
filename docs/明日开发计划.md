# 明日开发计划

## 📅 日期：2026-01-26

## 🎯 总体目标
实现核心LLM调用功能，让Agent能够真正生成内容，完成从"框架搭建"到"功能实现"的关键一步。

---

## 🔴 优先级1：核心功能实现（预计4-6小时）

### 1.1 实现统一的LLM客户端封装

**目标：** 创建统一的LLM客户端，支持调用DeepSeek/Qwen等国内API

**任务清单：**
- [x] 创建 `LLMClientConfig` 配置类
  - 创建 `ChatModel` bean（使用Spring AI的自动配置）
  - 配置模型参数（temperature、maxTokens等）
  - 支持DeepSeek的 `deepseek-chat` 模型
  
- [x] 创建 `LLMClient` 服务类
  - 封装 `ChatModel` 调用
  - 提供统一的prompt构建方法
  - 支持流式输出
  - 添加错误处理和重试机制

**文件位置：**
- `ai-agent-station-study-app/src/main/java/cn/bugstack/novel/config/LLMClientConfig.java`
- `ai-agent-station-study-infrastructure/src/main/java/cn/bugstack/novel/infrastructure/client/llm/LLMClient.java`

**参考代码：**
- 参考 `ai-agent-station-study` 项目中的 `ChatClient` 实现
- 使用 Spring AI 的 `ChatModel` 接口

---

### 1.2 在Agent中集成LLM调用 

**目标：** 替换所有Agent中的占位代码，实现真实的LLM调用

**任务清单：**
- [x] `NovelSeedAgent` - 实现Seed生成
  - 调用LLM生成小说标题、简介、核心设定
  - 使用RAG检索相似风格文本作为参考
  
- [x] `NovelPlannerAgent` - 实现小说规划
  - 调用LLM规划总卷数、每卷章节数
  - 生成整体大纲
  
- [x] `VolumePlannerAgent` - 实现卷规划
  - 调用LLM规划卷的章节结构
  - 生成卷级大纲
  
- [x] `ChapterOutlineAgent` - 实现章节梗概生成
  - 调用LLM生成章节标题和梗概
  - 提取关键人物和场景
  
- [x] `SceneGenerationAgent` - 实现场景正文生成 ⭐ 重点
  - 调用LLM生成场景正文（3000字左右）
  - 使用RAG检索相似风格场景
  - 支持FIM（Fill-in-the-Middle）补全
  
- [x] `ConsistencyGuardAgent` - 实现一致性校验
  - 调用LLM检查人物性格是否一致
  - 检查关系是否漂移

**关键文件：**
- `ai-agent-station-study-domain/src/main/java/cn/bugstack/novel/domain/agent/impl/planning/NovelSeedAgent.java`
- `ai-agent-station-study-domain/src/main/java/cn/bugstack/novel/domain/agent/impl/generation/SceneGenerationAgent.java`
- 其他Agent实现类

**注意事项：**
- 每个Agent需要注入 `LLMClient`
- Prompt需要精心设计，包含上下文信息
- 处理LLM返回的JSON格式数据
- 添加错误处理和降级策略

---

## 🟡 优先级2：功能完善（预计2-3小时）

### 2.1 知识图谱服务配置 ⏱️ 1-2小时

**目标：** 配置Neo4j知识图谱服务，支持KG相关Agent

**任务清单：**
- [ ] 检查Neo4j连接配置
  - 确认 `application-dev.yml` 中的Neo4j配置正确
  - 测试Neo4j连接
  
- [ ] 实现 `Neo4jKnowledgeGraphService`
  - 实现 `IKnowledgeGraphService` 接口
  - 实现人物创建、关系创建、查询等功能
  - 使用Cypher查询语言
  
- [ ] 验证 `ConsistencyGuardAgent` 和 `KGRuleValidatorAgent`
  - 确保KG服务正常工作
  - 测试规则校验功能

**文件位置：**
- `ai-agent-station-study-infrastructure/src/main/java/cn/bugstack/novel/infrastructure/adapter/kg/Neo4jKnowledgeGraphService.java`

---

### 2.2 完善错误处理和日志 ⏱️ 1小时

**任务清单：**
- [ ] 添加统一的异常处理
  - 创建自定义异常类
  - 添加全局异常处理器
  
- [ ] 完善日志记录
  - 记录LLM调用参数和响应
  - 记录Agent执行时间
  - 记录错误信息

---

## 🟢 优先级3：测试和验证（预计1-2小时）

### 3.1 端到端测试 ⏱️ 1-2小时

**任务清单：**
- [ ] 编写完整流程测试
  - 测试从Seed到章节生成的完整流程
  - 验证每个Agent的输出质量
  
- [ ] 测试RAG功能
  - 添加测试文档到向量库
  - 测试检索功能
  
- [ ] 测试LLM调用
  - 验证DeepSeek API调用正常
  - 检查生成内容质量

**测试文件：**
- `ai-agent-station-study-app/src/test/java/cn/bugstack/novel/test/NovelGenerationFlowTest.java`

---

## 📋 开发检查清单

### 开始开发前
- [ ] 确认DeepSeek API Key有效
- [ ] 确认PostgreSQL + pgvector正常运行
- [ ] 确认Neo4j正常运行（如果使用KG功能）
- [ ] 拉取最新代码

### 开发过程中
- [ ] 每个功能完成后立即测试
- [ ] 提交代码前运行测试
- [ ] 记录遇到的问题和解决方案

### 开发完成后
- [ ] 运行完整测试套件
- [ ] 检查日志是否有错误
- [ ] 验证生成的内容质量
- [ ] 更新文档

---

## 🎯 预期成果

**完成优先级1后：**
- ✅ 所有Agent能够调用LLM生成真实内容
- ✅ 可以完成从Seed到章节生成的完整流程
- ✅ 生成的内容质量达到可用水平

**完成优先级2后：**
- ✅ KG服务正常工作
- ✅ 一致性校验和规则校验功能可用

**完成优先级3后：**
- ✅ 有完整的测试覆盖
- ✅ 系统稳定可靠

---

## 📝 注意事项

1. **API调用成本：** DeepSeek API有调用成本，注意控制测试频率
2. **Token限制：** 注意DeepSeek的token限制（输入+输出）
3. **错误处理：** LLM调用可能失败，需要完善的错误处理
4. **内容质量：** 生成的Prompt需要精心设计，影响生成质量
5. **性能优化：** 后续可以考虑缓存、批量调用等优化

---

## 🔄 后续计划（优先级4）

如果时间充裕，可以考虑：
- 实现更多Agent（CombatDetailAgent、EmotionAndCPInteractionAgent等）
- 实现HumanInTheLoopAgent
- 添加Prometheus监控
- 优化性能和缓存

---

## 📚 参考资源

- Spring AI文档：https://docs.spring.io/spring-ai/reference/
- DeepSeek API文档：https://platform.deepseek.com/api_docs
- 参考项目：`ai-agent-station-study` 中的ChatClient实现
